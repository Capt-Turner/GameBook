<div class="container text-center">
    <div class="row">
        <div class="col-2">    
        </div>
    </div>
        <div class="col-10">
            <h1>Welcome to GameBook!</h1>
            <p>Here's ten random games to look at!</p>
        </div>
        
        <div class="col-2">    
        </div>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        {{#each gameData as |game|}}
        {{>game-details}}
        {{/each}}
    </div>
</div>